# SAMRAKSHA backend dependencies
# Last security audit: 2026-07-27
# All packages version-pinned with minimum safe versions per CVE advisories

# ─── Core API ────────────────────────────────────────────────────────────────
# fastapi>=0.111.0 — CVE-2024-24762 (python-multipart regex bypass) fixed
# starlette>=0.40.0 — CVE-2024-47874 (multipart DoS / uncontrolled memory) fixed
fastapi>=0.111.0
starlette>=0.40.0
uvicorn[standard]>=0.29.0
# python-multipart>=0.0.12 — CVE-2024-24762 fix
python-multipart>=0.0.12

# ─── Database ────────────────────────────────────────────────────────────────
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
redis>=5.0.0

# ─── Security & Auth ─────────────────────────────────────────────────────────
# PyJWT replaces python-jose — python-jose has unfixed CVEs:
#   CVE-2024-33663 (algorithm confusion / token forgery)
#   CVE-2024-33664 (JWT bomb DoS via high-compression JWE)
#   Library is unmaintained. PyJWT is the industry-standard replacement.
PyJWT[crypto]>=2.8.0
passlib[bcrypt]>=1.7.4
cryptography>=42.0.0
slowapi>=0.1.9

# ─── Process Manager ─────────────────────────────────────────────────────────
# gunicorn>=22.0.0 — GHSA-jgfp-53c3-624m, GHSA-w3h3-4rj7-4ph4 (HTTP request smuggling)
gunicorn>=22.0.0

# ─── ML & Prediction ─────────────────────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.4.0
scipy>=1.12.0
numpy>=1.26.0
pandas>=2.2.0

# ─── Patrol Routing ──────────────────────────────────────────────────────────
# (Routing delegated to OSRM docker container)

# ─── Document Generation ─────────────────────────────────────────────────────
python-docx>=1.1.0
python-magic>=0.4.27

# ─── CCTV / Computer Vision ──────────────────────────────────────────────────
mediapipe>=0.10.0
opencv-python-headless>=4.9.0.80

# ─── HTTP Client ─────────────────────────────────────────────────────────────
# httpx>=0.27.0 — CVE-2021-41945 (SSRF URL parsing) fixed in 0.23.0; 0.27+ recommended
httpx>=0.27.0

# ─── WebSocket ───────────────────────────────────────────────────────────────
websockets>=12.0

# ─── Geocoding ───────────────────────────────────────────────────────────────
# (Geospatial queries delegated natively to PostGIS)

# ─── Validation ──────────────────────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0

# ─── NLP / AI (voice, translation, assistant) ────────────────────────────────
# transformers: keep <4.40.0 for IndicTrans2 compatibility but update for security
# Note: transformers has multiple CVEs (CVE-2024-11393, CVE-2024-11394, etc.)
# Mitigated by: only loading models from internal/trusted sources (not user-supplied)
transformers>=4.40.0
torch>=2.2.0
openai-whisper
sentencepiece>=0.2.0
# IndicTrans2 companion tokenizer (required — not on PyPI)
git+https://github.com/VarunGumma/IndicTransTokenizer

# ─── Logging ─────────────────────────────────────────────────────────────────
structlog>=24.1.0
geopy>=2.4.1
docxtpl>=0.16.0
